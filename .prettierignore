node_modules/
dist/
.turbo/
.wrangler/
.claude/worktrees/
pnpm-lock.yaml
*.tsbuildinfo
.env*

# Release Please owns this file and regenerates its canonical Markdown on each
# release update. Formatting it locally would only create automation churn.
CHANGELOG.md

# Generated by `wrangler types` from wrangler.toml — 550KB of runtime
# typings. Regenerate it, never hand-edit it.
apps/worker/worker-configuration.d.ts


# Generator templates. Prettier infers the `glimmer` parser from .hbs and
# reformats them as Handlebars markup, which collapses every newline in what
# is actually a TypeScript file with placeholders in it. The generated output
# is formatted; the templates are not the artifact.
*.hbs
