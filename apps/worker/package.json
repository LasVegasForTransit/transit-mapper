{
  "name": "@transitmapper/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc -b tsconfig.build.json --noEmit && tsc -b tsconfig.json --noEmit",
    "test": "tsx tests/verify.test.ts && vitest run",
    "types": "wrangler types",
    "check:types": "tsx scripts/check-generated-types.ts",
    "validate": "pnpm check:types"
  },
  "dependencies": {
    "@transitmapper/core": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@lvbt/eslint-config": "github:LasVegasForTransit/repository-tooling#v0.2.1&path:/packages/eslint-config",
    "@lvbt/typescript-config": "github:LasVegasForTransit/repository-tooling#v0.2.1&path:/packages/typescript-config",
    "@lvbt/vitest-config": "github:LasVegasForTransit/repository-tooling#v0.2.1&path:/packages/vitest-config",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
