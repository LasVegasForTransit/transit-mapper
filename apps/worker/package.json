{
  "name": "@transitmapper/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc -b --noEmit && tsc -p tsconfig.scripts.json",
    "verify": "tsx scripts/verify.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@transitmapper/core": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "tsx": "^4.23.1",
    "wrangler": "^4.114.0"
  }
}
