// This entry names the complete runtime needed after the installed editor
// reloads without a network. The build manifest carries the dependency graph;
// the PWA policy never needs to know which component currently owns the map.
export { EditorMapSurface } from '../map/editor-map-surface';
