<!--
`pnpm check` is the bar and CI runs it, so there is no checklist here for
things a command already verifies. What is left is what a machine cannot
judge.
-->

## What this changes, and why

<!-- The why is the part the diff cannot show. What constraint, trade-off or
     failure does this address? -->

## How it was checked

<!-- `pnpm check` passing is assumed. If the change affects how something
     looks or behaves in the editor, say how you confirmed it — that is the
     part CI cannot run. -->

## Anything a reviewer should push back on

<!-- Shortcuts taken, alternatives rejected, anything you are unsure about.
     Saying so here costs less than it does in review. -->
