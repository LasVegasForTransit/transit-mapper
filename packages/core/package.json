{
  "name": "@transitmapper/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./testing/fixtures": "./tests/support/fixtures.test.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check-types": "tsc -b tsconfig.build.json --noEmit && tsc -b tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "fflate": "catalog:"
  },
  "devDependencies": {
    "@lvbt/typescript-config": "catalog:",
    "@lvbt/vitest-config": "catalog:",
    "@types/geojson": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}
