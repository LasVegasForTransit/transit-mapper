/** Open Graph's recommended 1.91:1 card size. Kept in a dependency leaf so
 * browser canvas encoding can share the contract without importing the SVG
 * composition engine that runs in its dedicated Worker. */
export const PREVIEW_WIDTH = 1200;
export const PREVIEW_HEIGHT = 630;
