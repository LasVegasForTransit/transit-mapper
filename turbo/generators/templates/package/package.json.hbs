{
  "name": "@transitmapper/{{name}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc -b tsconfig.build.json --noEmit && tsc -b tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@lvbt/typescript-config": "file:../../.lvbt/web-platform/packages/typescript-config",
    "@lvbt/vitest-config": "file:../../.lvbt/web-platform/packages/vitest-config",
    "vitest": "catalog:"
  }
}
