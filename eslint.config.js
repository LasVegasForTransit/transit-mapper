import { config } from '@lvbt/eslint-config/base';

// The scripts under scripts/ and the generators under turbo/ are Node
// programs; every package lints itself from its own directory.
export default config;
